<h3>Comments</h3>

<% @post.comments.each do |comment| %>
  <div class="well">
       <%= comment.body %>
       <%= comment.user_id %>

        <%= link_to '[X]', [comment.post, comment],
            method: :delete,
            data: {confirm: 'Are you sure?'} %>
            
    </p>
  </div>
<%end%>
